/* Let the herd settle in spring, then flip the calendar to summer so the
   migration actually has to happen in front of the camera. */
setTimeout(() => { window.__RC_WX__.jumpDay(1); }, 3000);
'scheduled jump to summer at t+3s'
